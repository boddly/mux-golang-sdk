package livestreams

import "github.com/boddly/mux-golang-sdk/video/playbacks"

// CreatePlayback creates a live stream playback ID
func (ls *LiveStream) CreatePlayback(ID, policy string) (*playbacks.IDs, error) {
	return nil, nil
}
