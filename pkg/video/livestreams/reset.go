package livestreams

// Reset a live stream key if you want to immediately stop the current stream key from working
// and create a new stream key that can be used for future broadcasts.
func (ls *LiveStream) Reset(ID string) (*LiveStream, error) {
	return nil, nil
}
